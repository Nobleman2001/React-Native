import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function TabTwoScreen() {
  return (
    <SafeAreaView className="items-center justify-center py-20">
      <Text className="text-3xl">Welcome to the explore</Text>
    </SafeAreaView>
  );
}
